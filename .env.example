# ---------- 演示：不接后端 ----------
# 下面整行留空或删掉，npm run dev 后右侧为「演示模式」占位回复
# VITE_AGENT_URL=

# ---------- 方式 A：直连你的 API（需 CORS）----------
# VITE_AGENT_URL=https://你的域名/v1/chat

# ---------- 方式 B：本地代理（配合 scripts/mock_agent_server.py）----------
# VITE_AGENT_URL=/api/agent
# VITE_AGENT_PROXY_TARGET=http://127.0.0.1:8000
# VITE_AGENT_PROXY_PATH=/chat

# ---------- 方式 C：腾讯云 LKE（HTTP SSE）----------
# 勿把 LKE_BOT_APP_KEY 写进 VITE_ 前缀变量，会暴露到浏览器。
# 终端先 export，再启动代理：
#   export LKE_BOT_APP_KEY="控制台的应用 AppKey"
#   python3 scripts/tencent_lke_proxy.py
# 前端仍用方式 B 三行（与 mock 相同端口时二选一启动）
